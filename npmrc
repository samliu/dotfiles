# Set TMP directory for use when npm is installing stuff.
# npm config set tmp=/tmp
tmp = /tmp

# Install global npm packages in the user's local folder.
# npm config set prefix '~/.npm-global'
prefix = ~/.npm-global
